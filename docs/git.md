## 12. Git

git flow: main → develop → feature/*, release/*, hotfix/*.
git blame: кто и когда менял каждую строку.
git rebase vs merge: rebase переписывает историю (чище); merge сохраняет (безопаснее для shared веток).
git bisect: бинарный поиск по коммитам для нахождения бага.

---
