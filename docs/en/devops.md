## 11. DevOps

### Docker

Image — immutable filesystem snapshot + metadata.
Container — running instance of an image; isolated via namespaces + cgroups.
Key Dockerfile instructions: `FROM`, `RUN`, `COPY`, `ENV`, `EXPOSE`, `CMD`, `ENTRYPOINT`.
Multi-stage builds: small final image without build-time dependencies.

---
