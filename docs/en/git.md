## 12. Git

git flow: `main` → `develop` → `feature/*`, `release/*`, `hotfix/*`.
`git blame`: who changed each line and when.
`git rebase` vs `merge`: rebase rewrites history (cleaner); merge preserves it (safer for shared branches).
`git bisect`: binary search through commits to find a bug.

---
